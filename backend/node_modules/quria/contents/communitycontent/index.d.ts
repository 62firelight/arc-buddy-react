import { APIResponse } from "../../types/api";
import { PostSearchResponse } from "../../types/interface";
export declare class CommunityContent {
    private url;
    private headers;
    constructor(url: string, headers: {
        [key: string]: string;
    });
    /**
     * Returns community content.
     * @param mediaFilter The type of media to get
     * @param page Zero based page
     * @param sort The sort mode.
     * @returns Community content.
     */
    GetCommunityContent(mediaFilter: number, page: number, sort: string): Promise<APIResponse<PostSearchResponse>>;
}
